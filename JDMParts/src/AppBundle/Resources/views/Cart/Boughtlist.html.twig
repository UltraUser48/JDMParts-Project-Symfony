{% extends '::base.html.twig' %}

{% block body %}
    <h1>List of the bought carts</h1>

    {% for cart_product in cart %}
    <h3>-> Cart ID:{{ cart_product.id}}</h3>
        <h4>Bought By:{{ cart_product.userid }}</h4>
{% endfor %}
{% endblock %}

